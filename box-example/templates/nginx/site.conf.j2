server {
  listen 80 default_server;
  server_name {{custom_demo_domain}};

  location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_set_header Host $host;
  }

  add_header X-Frame-Options DENY;
  add_header X-Content-Type-Options nosniff;
}


